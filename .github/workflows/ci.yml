name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: macos-26
    timeout-minutes: 30
    steps:
      # Always first, always runs, pure shell echo — costs nothing even if every
      # later step fails or gets skipped.
      - name: Regression-Testhinweis
        run: |
          echo "⚠️ Wichtig: CI verhindert Regressionen nur zusammen mit Tests — für neue Features bitte jeweils einen passenden Unit-Test in DokutresorTests/ ergänzen, sonst greift die CI nicht." >> "$GITHUB_STEP_SUMMARY"

      - uses: actions/checkout@v4

      # NOTE: dieses Projekt nutzt XcodeGen (project.yml) nur lokal zum Scaffolding.
      # Das eingecheckte Dokutresor.xcodeproj ist die Quelle der Wahrheit (siehe README) —
      # CI führt bewusst KEIN `xcodegen generate` aus und baut das eingecheckte Projekt
      # so, wie es ist.

      - name: Select latest available Xcode
        run: |
          LATEST=$(ls -d /Applications/Xcode_*.app 2>/dev/null | sort -V | tail -1)
          if [ -z "$LATEST" ]; then LATEST="/Applications/Xcode.app"; fi
          echo "Using $LATEST"
          echo "DEVELOPER_DIR=$LATEST/Contents/Developer" >> "$GITHUB_ENV"

      - name: Show environment
        run: |
          xcodebuild -version
          xcodebuild -list -project Dokutresor.xcodeproj

      - name: Install xcpretty
        run: gem install xcpretty --no-document

      - name: Pick newest available iPhone simulator
        id: sim
        run: |
          DEVICES=$(xcrun simctl list -j devices available)
          RUNTIME=$(echo "$DEVICES" | jq -r '.devices | keys | map(select(test("iOS"))) | sort | last')
          UDID=$(echo "$DEVICES" | jq -r --arg rt "$RUNTIME" '.devices[$rt] | map(select(.isAvailable and (.name | test("^iPhone")))) | .[0].udid')
          NAME=$(echo "$DEVICES" | jq -r --arg rt "$RUNTIME" '.devices[$rt] | map(select(.isAvailable and (.name | test("^iPhone")))) | .[0].name')
          if [ -z "$UDID" ] || [ "$UDID" = "null" ]; then
            echo "No available iPhone simulator found" >&2
            exit 1
          fi
          echo "Using $NAME ($UDID) on runtime $RUNTIME"
          echo "udid=$UDID" >> "$GITHUB_OUTPUT"

      - name: Build and test
        run: |
          set -o pipefail
          xcodebuild test \
            -project Dokutresor.xcodeproj \
            -scheme Dokutresor \
            -destination 'platform=iOS Simulator,id=${{ steps.sim.outputs.udid }}' \
            -only-testing:DokutresorTests \
            -resultBundlePath TestResults.xcresult \
            -enableCodeCoverage YES \
            -skipMacroValidation \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=YES \
            AD_HOC_CODE_SIGNING_ALLOWED=YES \
            | xcpretty

      - name: Coverage summary
        if: always()
        run: |
          if [ -d TestResults.xcresult ]; then
            {
              echo "### Code Coverage"
              echo '```'
              xcrun xccov view --report --only-targets TestResults.xcresult
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload xcresult on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: TestResults.xcresult
          path: TestResults.xcresult
          retention-days: 7
